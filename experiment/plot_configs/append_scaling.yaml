# Plot: append_scaling
# Append throughput vs. primary worker threads, one curve per
# aof_physical_sublog_count (evaluation plan v3, sec:6.2).

name: append_scaling
description: "Append throughput vs. primary worker threads (sec:6.2)."

dependencies: aof_enqueue_random

scale: 0.5
xticks: [1, 8, 16, 32, 64]
yticks: [0, 20, 40, 60, 80]
legend_separate: true

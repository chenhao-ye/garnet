# Experiment: replication_m16_tighten
# At 32 writers / m=16 the replica fully catches up at 600us drift (lag 0, 5.4ms
# freshness, reader p50 1.29ms). Reader p50 tracks the drift threshold, so tighten it
# and spend the replay slack on read latency: sweep {10us, 60us, 100us, 300us}.
# Deployment config held: 3 machines, no pinning, fine feed (1ms/256k), 1g buffers,
# ship 512m, replica throttle 256m.
name: replication_m16_60us_unclient
description: "60us arm, all pinned except client: which role carries the placement lottery?"
benchmark: replication
no_server: true
repeat: 3
remote:
  primary: { ssh: node0, ip: 10.10.1.1 }
  replica: { ssh: node1, ip: 10.10.1.2 }
  client:  { ssh: node2, ip: 10.10.1.3 }
affinity:
  primary: { numa_node: 0 }
  replica: { numa_node: 0 }
  client:  { numa_node: 0 }
base:
  primary_params: { port: 7379, cluster: true, aof: true, aof_null_device: true, fast_aof_truncate: true, aof_commit_freq: -1, repl_diskless_sync: true, aof_memory: 1g, aof_physical_sublog_count: 16, aof_ship_max_lag: 536870912, replica_sync_delay: 1, aof_replication_chunk_size: 262144 }
  replica_params:  { port: 7380, cluster: true, aof: true, aof_null_device: true, fast_aof_truncate: true, aof_commit_freq: -1, repl_diskless_sync: true, aof_memory: 1g, aof_replay_task_count: 1, aof_physical_sublog_count: 16, replica_offset_max_lag: 268435456, replica_sync_delay: 1, aof_replication_chunk_size: 262144 }
  client_params:   { replication_bench: true, client: GarnetClientSession, primary_port: 7379, replica_port: 7380, keylength: 8, valuelength: 8, dbsize: 1000000, replication_writers: 32, replication_readers: 1, itp: 256, runtime: 30 }
sweep_combo:
  - replica_params: { aof_replay_drift_threshold: 144000 }
